---
title: "Use isar_community 3.3.2, not 4.x"
date: 2026-08-10
tags:
  - adr
description: The project pins isar_community at 3.3.2 because the original Isar repo is unmaintained and 4.x does not exist for the community fork.
---

# 0015 — Use isar_community 3.3.2, not 4.x

**Status:** Accepted, 2026-08-10

## Context

The original Isar repo is unmaintained (`Original-Repo unmaintained`), while the community fork is active (`Community-Fork aktiv`).

## Decision

Use `isar_community` 3.3.2 (`nicht 4.x — existiert nicht`).

## Consequences

4.x is rejected as an alternative because it does not exist for the community fork; the newest stable release is 3.3.2 (`neueste stable ist 3.3.2`).

## Evidence

> [!NOTE]
> Not cited. Migrated from `decisions.md`, which was kept without `file:line` references.
> Verify against current code before acting on this record.
