---
title: "Use syncfusion_flutter_pdf for PDF text extraction"
date: 2026-08-11
tags:
  - adr
description: syncfusion_flutter_pdf was chosen for PDF text extraction; its Syncfusion Community License is not OSS but was accepted for this private, single-user app.
---

# 0034 — Use syncfusion_flutter_pdf for PDF text extraction

**Status:** Accepted, 2026-08-11

## Context

PDF import (0006) needs a library to extract text from bank statement PDFs.

## Decision

`syncfusion_flutter_pdf` for PDF text extraction (`für PDF-Text-Extraktion`).

## Consequences

This was a ticket choice (`Ticket-Wahl`). The license is the Syncfusion Community License, which is not OSS (`nicht OSS`), but was accepted for a private, single-user app (`für private Single-User-App akzeptiert`).

## Evidence

> [!NOTE]
> Not cited. Migrated from `decisions.md`, which was kept without `file:line` references.
> Verify against current code before acting on this record.
