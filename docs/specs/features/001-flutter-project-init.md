# Flutter project init

| Field | Value |
|-------|-------|
| **Type** | Feature |
| **Epic** | Setup |
| **Domain** | Infra |
| **Blocked By** | None |
| **Status** | Done |

## Description
Bootstrap Flutter project in the empty repo. Android-only target. Feature-first folder layout. Riverpod as state management. `flutter_lints` for static analysis.

## Acceptance Criteria
- [x] Flutter project scaffolded, Android target only (`--platforms=android`)
- [x] `applicationId` = `de.lorkaps_apps.budget_view` in `android/app/build.gradle.kts`
- [x] App display name = `BudgetView` (Android manifest `android:label`). Note: `pubspec.yaml` `name` stays `budget_view` — Dart package names must be snake_case; display name is the manifest label
- [x] Folder layout follows feature-first: `lib/core/` + `lib/features/` established (+ `lib/main.dart`); per-feature `{presentation,domain,data}` created per feature ticket
- [x] `flutter_lints` configured in `analysis_options.yaml` (default rules active)
- [x] `flutter_riverpod` (^2.6.1) added, `ProviderScope` wraps app root in `main.dart`
- [x] `flutter analyze` reports 0 issues (`make check` → "no issues")
- [x] Widget smoke test passes (`make test` green) — app boots in test harness
- [x] `.gitignore` includes Flutter/Dart standard entries (generated by `flutter create`)
- [~] `flutter run` on a physical emulator: NOT executed in this environment (Flutter blocked in agent sandbox). Verified indirectly via passing widget test + clean analyze. User can confirm live with `make run`.

## Affected Tests
- Default widget smoke test (`test/widget_test.dart`) passes on scaffold

## Fixtures Needed
No.

## Refinement Tokens (estimate)
- Input: ~8k tokens
- Output: ~2k tokens

## Implementation Tokens (estimate)
- Input: ~15k tokens
- Output: ~3k tokens
