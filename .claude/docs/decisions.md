# Architectural Decisions

| Date | Decision | Reason |
|------|----------|--------|
| 2026-08-10 | Flutter as app framework | User choice |
| 2026-08-10 | Isar as local persistence (local-first) | User choice |
| 2026-08-10 | Supabase as future sync layer (prep only) | User choice, cloud-sync deferred |
| 2026-08-10 | Google ML Kit for OCR | User choice, on-device |
| 2026-08-10 | Linear regression for forecast | User choice, simple + explainable |
| 2026-08-10 | Plug-in system for PDF parsers | Generic import, no bank lock-in |
| 2026-08-10 | Fractal categorization (line-item overrides parent) | User requirement, Kassenbon-drilldown |
| 2026-08-10 | Duplicate detection via hash (amount+date+description) | User requirement, PDF↔manual + PDF↔PDF |
| 2026-08-10 | Riverpod as state management | Compile-safe, DI included, fits Isar reactive patterns |
| 2026-08-10 | Android-only target (no iOS/Web/Desktop) | User scope reduction |
| 2026-08-10 | Feature-first folder layout | Fits domain-driven ticket structure |
| 2026-08-10 | flutter_lints (default) as linter | Sanft, ausreichend, kann später verschärft werden |
| 2026-08-10 | Package = `de.lorkaps_apps.budget_view`, App name = `BudgetView` | User choice |
| 2026-08-10 | KEINE DB-Encryption; Verlass auf Android FBE + App-Sandbox | Isar bietet keine eingebaute Encryption; Field-Encryption würde Queries/Sortierung brechen (Kern der App). Threat-Model für lokale Single-User-App durch OS + App-scoped storage abgedeckt. App-Lock (Biometrie/PIN) optional als späteres Ticket |
| 2026-08-10 | `isar_community` 3.3.2 (nicht 4.x — existiert nicht) | Original-Repo unmaintained, Community-Fork aktiv; neueste stable ist 3.3.2 |
| 2026-08-10 | Dev: nuke+rebuild bei Schema-Änderung. Prod: manuelle Migration ab v1.0 | Frühphase-Overhead klein, sauber ab Release |
| 2026-08-10 | Sync-Stub = Interface + lokale Change-Queue (Op-Log) | Validiert Design ohne Cloud-Anbindung |
| 2026-08-10 | Change-Queue-Eintrag: {op, entity_type, entity_id, payload_json, ts} | Fein-granular, standard local-first |
| 2026-08-10 | Entity-IDs = UUID v4 (client-generiert) | Kollisionsfrei über Geräte, Isar auto-inc bleibt interner Storage-Index |
| 2026-08-10 | Repository-Layer hook: jede Mutation ruft adapter.enqueue() | Zentrale Sync-Anbindung, keine App-weite Interceptor-Magie |
| 2026-08-10 | Geldbeträge = int64 Cents | Vermeidet Floating-Point-Fehler in Finance-Domain |
| 2026-08-10 | Currency = EUR only für MVP (kein Feld) | Reduziert Komplexität, Multi-Currency später als eigenes Ticket |
| 2026-08-10 | Soft-Delete für Account (archived Flag) | Historie bleibt erhalten, sync-freundlich |
| 2026-08-10 | Kategorie-Baum: freie Roots, Sign des Betrags = Richtung | Vermeidet Kategorie-Duplikate für Rückzahlungs-Cases (Pfand, Nebenkosten) |
| 2026-08-10 | Category Delete blockiert wenn Kinder oder Transaktionen vorhanden | Erzwingt explizites Umziehen, kein Datenverlust |
| 2026-08-10 | Category Name unique per Parent (nicht global) | Wiederverwendung von "Einkauf" / "Rückerstattung" unter versch. Parents |
| 2026-08-10 | Transaktion: Soft-Delete (deleted Flag) | Historie bleibt, sync-freundlich |
| 2026-08-10 | Dedupe-Hash: SHA-256 über amountCents + bookingDate (Datum) + normalisierter counterparty | User-Wahl; Kollisionen bei leerem counterparty werden als UI-Warnung dargestellt, User entscheidet |
| 2026-08-10 | Roh-Dokumente (PDFs + Kassenbon-Fotos) werden NICHT persistiert | Nur einmal ausgewertet, Bytes verworfen; nur Extrakt (Transaktionen / Line-Items) + Metadata bleiben |
| 2026-08-10 | ImportedSource-Entity: contentHash SHA-256 + Metadata pro Import | Re-Import-Warnung ("diese Datei schon am ... importiert") — Deckt PDF- + Foto-Imports |
| 2026-08-11 | Parser choice per PDF via confidence ranking (canParse 0.0–1.0), user may override top pick | No bank lock-in; heuristics can be wrong |
| 2026-08-11 | Parser that throws or times out in canParse is skipped, not fatal | One broken plug-in must not block whole import |
| 2026-08-11 | `file_selector` (not `file_picker`) as file picker | Official Flutter package; Android-only scope needs only single-file pick; smaller native surface |
| 2026-08-11 | `syncfusion_flutter_pdf` für PDF-Text-Extraktion | Ticket-Wahl; Lizenz = Syncfusion Community License (nicht OSS), für private Single-User-App akzeptiert |
| 2026-08-11 | Keine Fixture-PDFs; Parser-Verifikation über Saldo-Abstimmung an echten Auszügen (env-gated Harness) | Ein mit dem `pdf`-Paket erzeugtes PDF reproduziert ING's Textlayer-Eigenheiten nicht (gepolsterte Wörter, geschluckte Klammern, gesplittete TextLines) — es würde einen Pfad prüfen, den der echte Extraktor nie nimmt. Layout-Logik hängt stattdessen an synthetischen Wort-Koordinaten. Realdaten bleiben außerhalb von git |
| 2026-08-11 | ING-Spaltengrenzen aus der Kopfzeile jeder Seite ableiten, nicht hart kodieren | Layout-Änderung der Bank degradiert zu einer Warnung statt zu stillem Fehl-Parsing |
| 2026-08-11 | Zeilengrammatik ohne Whitelist der Buchungsarten (Datum+Betrag = neue Zeile, nacktes Datum = Valuta) | Die Liste der ING-Buchungsarten ist offen; eine Whitelist würde unbekannte Typen unbemerkt verschlucken |
| 2026-08-11 | Dedupe bleibt komplett in Ticket 009, 008 importiert nur | 008-ACs verlangten 009-Artefakte, während 009 auf 008 blockte — Zyklus aufgelöst durch Schnitt "008 imports, 009 warns" |
| 2026-08-12 | "Nicht gesetzt" ist überall `null`, kein Empty-String-Sentinel. Ersetzt die 010-Entscheidung für `parentUuid = ''` | User-Wahl bei Ticket 011: `Transaction.categoryUuid` braucht dasselbe Konzept, und zwei Schreibweisen für dieselbe Bedeutung in einem Schema kosten mehr als der bis dahin ungetestete nullable Index. `Category.parentUuid` wurde nachgezogen |
| 2026-08-12 | Kategorie-Pflicht sitzt im Formular, nicht im Feld | Manuelle Erfassung verlangt eine Kategorie, PDF-Import erlaubt unkategorisierte Zeilen — dieselbe Spalte mit zwei Regeln je Eingabeweg |
| 2026-08-12 | `dedupeHash` wird bei *jedem* Save neu berechnet, nicht nur wenn leer | Sonst beschreibt der Hash nach einer Betrags-, Datums- oder Empfänger-Korrektur die alte Buchung, und die Duplikatprüfung vergleicht gegen etwas, das nicht mehr existiert |
| 2026-08-12 | `ImportedSource.delete` löscht echt, kein Soft-Delete wie sonst überall | Die Zeile existiert, damit ein Re-Import warnt. Eine archivierte Zeile, die weiter warnt, wäre sinnlos — Löschen *ist* hier die Fachfunktion |
| 2026-08-12 | Intra-Batch-Duplikate markieren **beide** Kopien | Die zweite ist nicht automatisch die falsche; der User entscheidet, welche bleibt |
| 2026-08-12 | Geteilte Import-Artefakte in eigener `Import`-Domain, formatspezifischer Code bleibt | `ImportedSource` und Doc-Hash brauchen PDF *und* Foto; der ING-Parser braucht nur PDF. Umziehen von funktionierendem 008-Code ohne funktionalen Gewinn vermieden |
| 2026-08-12 | Querschnitts-Services als Interface + `Local`-Implementierung (`SyncAdapter`, `DuplicateChecker`) | Erlaubt Widget-Tests ohne Isar, das in der Fake-Async-Zone von `testWidgets` nie fertig wird |
| 2026-08-13 | Line-Items sitzen als Sektion im `TransactionFormScreen` (Edit-Modus), kein eigener Detail-Screen | Das Formular besitzt die persistierte uuid, die eine Position braucht; ein Detail-Screen hätte nur einen Navigations-Hop und einen Umbau der Listen-Tests gekostet. Wird geprüft, wenn 016 den Kassenbon-Scan-Einstieg braucht |
| 2026-08-13 | `quantity × unitPrice ≠ amount` ist eine Warnung im Sheet, keine Repo-Ablehnung | Rabatt- und Pfandzeilen brechen das Produkt absichtlich. Folgt der Dedupe-Warnung, die auch im Formular sitzt statt im Repo |
| 2026-08-13 | Neue Collection ohne Bump von `kDbSchemaVersion` | Rein additiv — Isar legt die Collection beim Öffnen an. Ein Bump hätte in Dev die DB genukt und Testdaten gekostet, ohne Gegenwert |
| 2026-08-13 | Kategorie-Resolver liegt in `Drilldown`, nicht im von Ticket 012 genannten `Category`-Pfad | Die Funktionen nehmen ein `LineItem`; `Category` hängt nur an Infra plus der schmalen Transaction-Kante. Der Ticket-Pfad hätte `Category → Drilldown` neu aufgezogen, für eine Regel, deren Gegenstand die Position ist. Analytics (020, 022) hängt ohnehin an beiden Domains |
| 2026-08-13 | `reconcile()` wird von den UI-Pfaden gerufen, nicht per Hook in `TransactionRepository.save` | Ein Hook dort hätte `Transaction → Drilldown` gedreht, für eine Regel, die den Positionen gehört. Vier Aufrufstellen (Sheet-Save, Section-Delete, Section-Reorder, Buchungsformular) statt einer zentralen — Risiko: ein künftiger Schreibpfad auf `amountCents` vergisst den Aufruf. PDF-Import ist unkritisch, frische Buchungen haben keine Positionen |
| 2026-08-13 | Restposten-Schutz per `save()`-Guard + Test, nicht per Compiler | Dart hat kein package-private; Privatheit ist library-weit. Die Alternativen (Reconciler in dieselbe Datei wie das Repository, oder Sentinel-Token im Aufruf) kosten mehr Lesbarkeit als der Schutz wert ist |
| 2026-08-13 | Restposten-Zeile ist von Drag und Swipe ausgenommen, statt sie beim Versuch abzulehnen | Eine Zeile, die wegwischt und zurückkommt, liest sich als Bug. Reorder schreibt nur die regulären Zeilen, der Reconciler pinnt die verwaltete danach wieder nach unten |
| 2026-08-13 | `TaggingLearnService.learnFrom` wird von den UI-Pfaden gerufen, nicht per Observer in `TransactionRepository.save` | Gleiche Begründung wie beim Restposten-Reconcile: ein Hook im Repository hätte `Transaction → Tagging` gedreht. Drei Aufrufstellen (Formular, Inline-Quickpick, Import-Persist) — vergisst eine künftige Stelle den Aufruf, geht Lernen verloren, aber nichts wird inkonsistent |
| 2026-08-13 | `TaggingRule.delete` löscht echt, kein Soft-Delete | Eine archivierte Regel, die weiter vorschlägt, wäre sinnlos — dieselbe Logik wie bei `ImportedSource` |
| 2026-08-13 | `Transaction.categoryAutoSuggested` schon in 013 eingeführt, obwohl erst 014 es auf `true` setzt | Der Learn-Hook muss es lesen, sonst verstärkt sich ein akzeptierter Vorschlag selbst. Feld ist additiv, Altdaten lesen `false` — kein Schema-Bump |
| 2026-08-13 | Regel-Verwaltungs-UI aus 013 in ein eigenes Ticket 025 geschnitten | Es gibt noch keine Settings-Fläche; sie als Nebenprodukt eines Storage-Tickets zu bauen hätte sie versehentlich entworfen. 024 braucht dieselbe Fläche — wer zuerst landet, baut sie |
| 2026-08-13 | Parent-Soft-Delete kaskadiert nicht auf Line-Items | Positionen sind nur über die Buchung erreichbar, also genügt Unerreichbarkeit; ein Cascade würde die Wiederherstellung einer Buchung unvollständig machen |
| 2026-08-17 | OCR- und Parser-Contract entstehen in 016, die Implementierung erst in 017/018 (Stub statt Flow-Schnitt) | Der Controller *ist* das Artefakt von 016. Die Alternative — 016 endet nach dem Hash-Check — hätte die State-Machine zweimal umgebaut, und die Handoff-Contracts standen ohnehin schon im Ticket |
| 2026-08-17 | Scan-Einstieg als Button in `LineItemsSection`, kein Detail-Screen | Beantwortet den Prüfpunkt aus der 016-Klausel von #50: der Flow ist komplett modal und braucht keine eigene Screen-Fläche. Ein Detail-Screen hätte die von 015/019 gerade fertiggestellten Listen-Tests umgebaut |
| 2026-08-17 | Kein Provenance-Feld für "Positionen aus Scans", der geplante Zähler ist gestrichen | Ein rein informativer Zähler rechtfertigt kein Schema-Feld. Ehrlich wäre er nur pro Zeile (`LineItem.importedSourceUuid`) — das gehört nach 018, wo die Zeilen entstehen, und trägt dort echten Nutzen (Badge, Sammel-Löschen) |
| 2026-08-17 | Doc-Hash über die Rohbytes, Downscale erst danach | Sonst verschiebt ein Update der Image-Lib die Dokument-Identität, und die Re-Scan-Warnung aus 009 greift für dasselbe Foto nicht mehr |
| 2026-08-17 | Foto-Bytes außerhalb des Riverpod-States; Provider `autoDispose` plus `listenManual` für die Flow-Dauer | Bytes gehören nicht in einen vergleichbaren State. `autoDispose` erzwingt das Verwerfen beim Verlassen, die manuelle Subscription hält den Controller genau so lange am Leben wie den Flow. `state.holdsImage` spiegelt die Referenz, damit die Regel testbar ist |
| 2026-08-17 | Keine Android-Permission für den Scan | Photo Picker (API 33+) und `ACTION_IMAGE_CAPTURE` brauchen keine; ein deklariertes `CAMERA` würde die Runtime-Abfrage erst erzeugen. Grenze: `image_picker` hält für Kamera-Captures eine plugin-eigene Temp-Datei, die unser Code nie sieht |
| 2026-08-17 | Restposten-Reconcile schon im Confirm von 016, nicht erst in 018 | 016 schreibt bereits Positionen, und ein Schreibpfad ohne Reconcile hätte die Summen-Invariante aus 019 gebrochen. 018 erbt den Aufruf statt ihn nachzurüsten |
| 2026-08-17 | `OcrResult` trägt Layout (Blöcke, Zeilen, `Rect`), nicht nur flache Strings | ML Kit liefert die Koordinaten gratis, und Bon-Heuristik lebt von x-Positionen (Preise rechtsbündig, Rabatte eingerückt) — dieselbe Lehre wie bei den ING-Spaltengrenzen. Der Bruch des in 016 geschriebenen Contracts war billig, weil noch kein Konsument existierte; später wäre die verworfene Information teuer zurückzuholen |
| 2026-08-17 | Kein `OcrEmptyException`; ein leeres Ergebnis fließt durch, nur `OcrEngineException` bricht ab | "Null Positionen ist ein gültiger Scan" aus 016 bleibt damit gültig. Die Unterscheidung mit Wert ist "Engine kaputt" vs. "Bon unlesbar", nicht ein zweiter Exception-Typ; das Retry gehört zu 018, das die Review-Fläche baut |
| 2026-08-17 | OCR läuft über eine eigene, sofort gelöschte Temp-Datei statt über NV21-Konvertierung in Dart | `InputImage.fromBytes` verlangt auf Android rohes NV21 plus Rotation. Selbst konvertieren heißt YUV *und* EXIF-Rotation besitzen — ein Fehler dort liefert leise Buchstabensuppe statt eines Absturzes. Damit stimmt die 016-Zusage "unser Code schreibt nichts auf Platte" nicht mehr; die Zusage, die zählt (das Foto wird nicht behalten), hält durch das `finally` |
| 2026-08-17 | ML-Kit-Recognizer langlebig pro Provider, Freigabe über `ref.onDispose` | ML Kits eigene Empfehlung: eine native Allokation für die App-Lebenszeit statt einer pro Foto |
| 2026-08-17 | Keine automatisierte Verifikation der echten Texterkennung; Umlaute werden am Gerät geprüft | Das Plugin hat kein Binding in der Test-VM, und ein synthetisch gerendertes Bild prüft einen Pfad, den kein echter Thermodruck-Bon nimmt — dasselbe Argument, mit dem die Fixture-PDFs rausgeflogen sind. Unit-Tests decken das ab, was wir besitzen: Mapping, Fehler-Wrapping, Datei-Lebenszyklus |
| 2026-08-17 | Scan-Kandidaten tragen unsigned Magnituden, das Vorzeichen setzt der Flow beim Persistieren | Ein Bon kennt keine Vorzeichen, `LineItemValidation.amount` lehnt Negative ausdrücklich ab, und 015 hängt das Vorzeichen an die Buchung. Ersetzt den `transactionSign`-Parameter aus 016: eine Transformationsstelle statt zwei |
| 2026-08-17 | Zeilen-Gruppierung über Blockgrenzen hinweg, per y-Überlappung, Preis = rechtester Geldtoken | ML Kit trennt Artikel- und Preisspalte eines Bons oft in getrennte Blöcke; blockweises Parsen würde genau die Paarung zerreißen, um die es geht. Koordinaten statt Textmuster — dieselbe Linie wie bei den ING-Spaltengrenzen |
| 2026-08-17 | Einheitspreis nur wenn `amount / quantity` auf den Cent aufgeht, sonst `null` | Ein hergeleiteter Preis, den niemand gedruckt hat, sieht im Formular aus wie erfasste Daten. Der Mismatch bleibt eine Warnung im Sheet (Entscheidung vom 2026-08-13) |
| 2026-08-17 | Maßeinheit bleibt im Beschreibungstext, Stückzahl wird konsumiert | `LineItem` hat kein Einheitenfeld (015), die Einheit lebt in der Beschreibung. "1,5 kg Äpfel" würde sonst das kg verlieren, "2x Milch" hingegen trägt die 2 schon in `quantity` |
| 2026-08-17 | Nicht speicherbare Kandidaten werden beim Confirm übersprungen, nicht dem Repository vorgelegt | Der Review-Toggle ist deaktiviert, solange eine Zeile unvollständig ist. Ein `LineItemInvalid` mitten im Persistieren wäre derselbe Ausgang auf dem teureren Weg — mit halb geschriebenen Positionen |
| 2026-08-17 | Review ist ein eigener Screen innerhalb der Modal-Kette, kein weiterer Dialog | Zeilen editieren, ein-/ausschalten, kategorisieren passt nicht in einen Dialog. Der Screen gibt die Liste in den schon vorhandenen `confirm(edited:)`-Seam zurück; die `listenManual`-Subscription des Flows überlebt den Push, die Foto-Bytes also auch |
| 2026-08-18 | Report-Einstieg als `AppShell` mit `NavigationBar` + `IndexedStack`, nicht als Icon in der Konten-AppBar | User-Wahl bei Ticket 020. 021 (Forecast), 022 (Preistrends) und 024/025 (Settings) brauchen ohnehin eine Fläche; die AppBar hätte sie einzeln nicht getragen. `IndexedStack` statt Rebuild pro Tab-Wechsel, damit Filterstand und Scrollposition beider Tabs überleben |
| 2026-08-18 | Richtungsfilter (Ausgaben/Einnahmen) entscheidet an der **Buchung**, nicht an der einzelnen Position | Ein überschießender Restposten zeigt legitim gegen seine Geschwister (Entscheidung vom 2026-08-13). Positionsweise gefiltert wäre der +5-Restposten einer −50-Buchung in den Einnahmen-Report gewandert, und die Ausgabenseite hätte 55 statt 50 gemeldet |
| 2026-08-18 | Summen bleiben intern vorzeichenbehaftet, Magnituden erst an der Row-Kante (`abs()`) | Ein Teilbaum muss sich gegen sich selbst verrechnen können, sonst addiert der Rollup Betrag und Gegenbetrag zur doppelten Summe. Die Anzeige will trotzdem positive Zahlen |
| 2026-08-18 | Positionen eines Monats über eine `anyOf`-Bulk-Query (`findByTransactions`), nicht N × `findByTransaction` | Der Report rechnet bei jeder Änderung an vier Collections neu; N Queries pro Lauf wachsen mit der Historie. Eigener Isar-Query in `analytics/data` wäre schneller zu schreiben gewesen, hätte aber die Repo-Grenze gebrochen, die jede andere Domain einhält |
| 2026-08-18 | Eine `categoryUuid`, die auf keine existierende Kategorie zeigt, zählt als „Ohne Kategorie“ | Sonst verschwindet der Betrag lautlos aus dem Report — dieselbe Fehlerklasse, die der eigene Uncategorized-Block gerade sichtbar machen soll |
| 2026-08-18 | Im Drilldown erscheinen die eigenen Beträge des Parents als eigene Zeile „X (direkt)“ inkl. Donut-Segment | User-Wahl bei Ticket 020. Damit ist die Drilldown-Summe identisch mit dem `rollupCents` der angetippten Zeile; die Alternative (nur im Header) hätte Donut-Prozente auf eine andere Basis bezogen als die angezeigte Gesamtsumme |
| 2026-08-18 | Deutsche Monatsnamen fest in `date_format.dart`, kein `intl`-Locale-Datensatz | `DateFormat` mit Monatsnamen bräuchte `initializeDateFormatting`; ein fehlender Aufruf fliegt erst zur Laufzeit auf. Die Datei vermeidet intl schon bei `dd.MM.yyyy` |
| 2026-08-18 | Monatsreihe als `computeSeries()` am bestehenden Report-Service, nicht N × `compute()` und nicht als extrahierter Unit-Helper | User-Wahl bei Ticket 021. Resolver, Rollup, Richtung und Uncategorized bleiben in der Klasse, die sie schon besitzt; Buchungen, Positionen und Kategoriebaum werden einmal pro Serie geladen statt einmal pro Monat. `compute()` delegiert jetzt mit `windowMonths: 1` — ein Pfad statt zwei |
| 2026-08-18 | Zwei Einstiege in die Prognose: Shell-Tab **und** Deep-Link aus der Report-Zeile | User-Wahl bei Ticket 021. Der Tab macht sie direkt erreichbar, der Deep-Link übernimmt Kategorie, Konto, Richtung und Monat — damit können Report und Prognose nicht verschiedene Zahlen zur selben Kategorie zeigen |
| 2026-08-18 | Deep-Link per Long-Press auf die Report-Zeile, nicht als dritter Trailing-Button | Betrag + Chevron + Button in einem `ListTile.trailing` ist genau die Zeile, die schon einmal übergelaufen ist (errors.md). Long-Press ist in dieser App etabliert: `CategoryTreeScreen` archiviert so |
| 2026-08-18 | Prognosewerte unter 0 werden auf 0 geklemmt | Ein fallender Trend läuft irgendwann durch die Null; negative Ausgaben gibt es nicht. Die Reihe trägt Magnituden, ein negativer Punkt wäre kein Vorzeichen, sondern ein Denkfehler |
| 2026-08-18 | `r2 = 0` bei varianzfreier Reihe, nicht `1.0` und nicht `NaN` | Bei flacher Reihe ist `ssTot = 0`, die Formel liefert `0/0`. `1.0` würde als perfekte Anpassung gelesen, obwohl nichts erklärt wurde |
| 2026-08-18 | `windowMonths = null` heißt „ganze Historie", kein `-1`-Sentinel wie im Ticket vorgeschlagen | Folgt der Entscheidung vom 2026-08-12: „nicht gesetzt" ist in diesem Schema überall `null`. Zwei Schreibweisen für dieselbe Bedeutung kosten mehr als der Sonderfall |
| 2026-08-18 | `MonthlyReportPoint` trägt Jahr + Monat neben dem Report, statt sie in `MonthlyCategoryReport` zu legen | Ein Report weiß sonst nicht, welchen Monat er beschreibt, aber `MonthlyCategoryReport.empty` muss `const` bleiben — mit Monatsfeldern ginge das nicht |
| 2026-08-18 | Ein Bottom-Nav-Tab wird über Nutzungsfrequenz verdient; seltene Flächen liegen hinter `Mehr` → `MenuScreen`. Prognose ist mit umgezogen | Material 3 erlaubt 3–5 Ziele, und 022/024/025 hätten die Leiste auf sechs getrieben. Der teure Teil ist nicht das Menü, sondern das zweimalige Umziehen von Shell, Tests und Docs — deshalb vor 022 gebaut. Die Prognose verliert nichts: ihr natürlicher Weg ist der Long-Press aus der Report-Zeile |
| 2026-08-19 | Eine Alternative aus dem Vorschlags-Sheet zählt als **Override**, nicht als Annahme (`categoryAutoSuggested = false`) | `learnFrom` überspringt angenommene Vorschläge, damit sich ein Vorschlag nicht selbst verstärkt. Würde die bewusst gewählte Nummer zwei auch als Annahme gelten, bliebe ihr `hitCount` stehen und sie könnte den Spitzenreiter nie überholen — die Korrektur des Users wäre wirkungslos |
| 2026-08-19 | Vorschlag beim Verlassen des Empfänger-Felds, nicht pro Tastendruck | Jeder Lookup ist eine Isar-Query, und ein halb getippter Empfänger matcht ohnehin keine Regel. Gleiche Linie wie der Debounce der Artikel-Suche aus 022 |
| 2026-08-19 | Archivierte und verwaiste Kategorien fallen aus den Vorschlägen heraus | Der Picker bietet beide nicht an; ein Vorschlag, den der User nicht von Hand wiederholen kann, ist ein Angebot ins Leere. Die Regel selbst bleibt gespeichert und legal — 025 heilt sie |
| 2026-08-19 | `rowSuggestions` als Map im `ImportFlowState` (neben `rowMatches`), `categorySuggested` dagegen auf der `ImportRow` | Vorschläge sind abgeleitete Anzeigedaten pro Zeilenindex und folgen dem Muster, das `rowMatches` schon vorgibt. Die Provenance ist dagegen eine Eigenschaft der Zeile selbst: sie wandert beim Persistieren in `Transaction.categoryAutoSuggested` |
| 2026-08-19 | Artikel-Gruppierung über `normalizeForMatching`, keine Fuzzy-Zusammenführung von OCR-Varianten | Sorten sind echte Preisunterschiede (`h-milch 1,5 %` ≠ `h-milch 3,5 %`), und keine Heuristik trennt das von einem Scan-Tippfehler. Dieselbe Funktion wie Dedupe und Tagging, damit „derselbe Artikel" in allen drei Fällen dasselbe heißt. Ein Merge-Werkzeug wird erst gebaut, wenn echte Bondaten den Schmerz zeigen |
| 2026-08-19 | Long-Press auf eine Positionszeile öffnet die Preis-Historie — eine reine Navigations-Kante Drilldown → Analytics | User-Wahl bei Ticket 022. Folgt dem dokumentierten Präzedenzfall `Account → Category` (AppBar öffnet den Kategoriebaum, sonst keine Abhängigkeit). Die Alternative, den Callback über `TransactionFormScreen` einzuhängen, hätte die Kante nur auf `Transaction → Analytics` verschoben |
| 2026-08-19 | Die beiden Artikel-Provider sind `autoDispose`, die Report- und Prognose-Provider nicht | Ihr Family-Key ist Nutzertext (Suchstring bzw. Artikelbeschreibung), die Zahl der Keys wächst also mit dem Tippen. Bei den Report-Filtern ist sie durch die Feldkombination begrenzt |
| 2026-08-19 | Preis-Chart hat eine echte Zeitachse (Tage seit dem ersten Kauf) und verankert **nicht** bei 0 | Käufe liegen unregelmäßig; eine Index-Achse hätte gleichmäßige Abstände behauptet und damit einen Trend erfunden. Die Nullverankerung der Prognose passt hier nicht: 20 Cent auf einen 1,49-€-Artikel sind die ganze Geschichte und an der Nullachse unsichtbar |
| 2026-08-19 | Kein Empty-Key-Guard im Trend-Service | `LineItemRepository` validiert die Beschreibung bei jedem Write als nicht-leer, der Zweig ist über keinen erlaubten Schreibpfad erreichbar. Ihn zu testen hätte einen Roh-Isar-Write an der Repo-Grenze vorbei gebraucht — teurer als der Schutz wert ist, gleiche Linie wie die gelöschten `No*`-Fallbacks |
| 2026-08-17 | `NoOcrService` und `NoReceiptLineItemParser` gelöscht, statt als Fallback zu behaupten | Nach 017/018 referenziert sie niemand mehr, auch kein Test — die Fakes der Suites sind eigene Klassen. Toter Code, der als Sicherheitsnetz gelesen worden wäre |
| 2026-08-20 | PNG-Rasterung in-repo via `tool/svg_to_png.py` + `make icon-png`, statt manueller User-Export | SVGs bleiben einzelne Quelle; ein Design-Tweak ist ein `make`-Aufruf statt drei manueller Exporte. `make icons` und `make splash` hängen an `icon-png`, der Raster kann nicht veralten |
| 2026-08-20 | Ein Teal (`#009688`) für beide (hell+dunkel), kein `color_dark` | Die gefüllte Fläche trägt eigenständigen Kontrast; Mark liest gleich über hellen und dunklen Launcher-Wallpapers. Splash nutzt dasselbe Teal, Startup fließt ohne Farbsprung in die App. Eine Prüfung statt zwei |
| 2026-08-20 | Adaptive Framing lebt im SVG, Generator-Inset = 0 | Default-Inset (16 %) würde eine bereits für Safe-Zone gerahmte Mark verkleinern, im Launcher-Grid sichtbar zu klein. Framing steht damit an genau einer Stelle |
| 2026-08-20 | Echter Einstellungs-Screen hinter einem `Mehr`-Tile, statt die Datenlisten direkt auf den `Mehr`-Tab zu hängen | Konfiguration behält eine Heimat, sobald der App-Lock-Toggle dazukommt — sonst stehen Präferenzen und Datenlisten an verschiedenen Orten. Akzeptierte Kosten: drei Ebenen (`Mehr` → `Einstellungen` → Liste) für heute eine Zeile |
